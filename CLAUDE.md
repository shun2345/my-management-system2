# CLAUDE.md

このファイルは Claude Code への指示書です。リポジトリ内で作業するときは必ず最初にこのファイルを読み、ここに書かれたルールに従ってください。

> **編集者へ**: `<<<...>>>` で囲まれた箇所はあなたの環境に合わせて書き換えてください。書き換え後はこの注意書きを削除しても構いません。

---

## 1. プロジェクト概要

### 1.1 サイトの目的

40〜50代女性（サンドイッチ世代）に向けて、親の介護・相続・終活・家計に関する公的制度と一般情報を、短尺動画とブログ記事で発信するメディアを運営している。最終的にFP相談・相続相談・終活関連サービスへのアフィリエイト送客を主な収益源とする。

### 1.2 サイト基本情報

- サイト名: <<<例：介護とお金の制度ガイド>>>
- ドメイン: <<<example.com>>>
- 運営者名（表示名）: <<<運営者の表示名>>>
- 運営者の立場: 当事者・経験者ではなく、公的資料を元に制度を解説する「情報整理者」として発信する
- 保有資格: <<<なしの場合は空欄。終活カウンセラー2級、相続診断士など取得後に追記>>>

### 1.3 ターゲットペルソナ

- 年齢: 42〜55歳、女性中心
- 家族構成: 既婚、子供あり（中高生〜独立）、実家の親が70〜80代
- 主な悩み: 親の物忘れ、兄弟との介護話、家計と介護費の両立、相続揉め、親への終活提案の難しさ
- 情報収集行動: スマホで隙間時間にSNS、夜寝る前のベッドスクロール

### 1.4 コンテンツ方針の根本ルール

このプロジェクトの記事と動画は次の方針を**例外なく**守る：

1. **実体験を語らない**。「私が介護した」「義母を看取った」など個人体験の創作・記述は禁止
2. **公的資料ベースの解説**に徹する。厚労省・国税庁・内閣府・地域包括支援センター等の一次情報を出典として明示する
3. **個別具体的な金融助言・税務助言・法律助言をしない**。必ず「専門家への相談を促す」で締める
4. **断定表現を避ける**。「必ず」「絶対」「確実に」は使わない。「〜の場合があります」「〜と言われています」を使う
5. **ステマ規制（景表法）対応**。アフィリエイトリンクを含む記事には PR 表記を必須とする

これらは法律遵守と長期的な信頼構築のための絶対要件であり、トラフィック獲得のために緩めることはしない。

---

## 2. 技術スタック

### 2.1 採用技術

- フレームワーク: Next.js 15（App Router）
- 言語: TypeScript（strict mode）
- スタイル: Tailwind CSS + shadcn/ui
- コンテンツ管理: MDX + velite
- ホスティング: Vercel
- 解析: Vercel Analytics, Google Analytics 4, Google Search Console
- メール: <<<Resend or SendGrid>>>
- 画像: <<<Vercel Blob or Cloudflare R2>>>

### 2.2 ディレクトリ構成

```
.
├── app/
│   ├── (site)/
│   │   ├── page.tsx                  # トップページ
│   │   ├── posts/[slug]/page.tsx     # 記事詳細
│   │   ├── category/[slug]/page.tsx  # カテゴリ一覧
│   │   └── tags/[slug]/page.tsx      # タグ一覧
│   ├── about/page.tsx                # 運営者情報
│   ├── privacy/page.tsx              # プライバシーポリシー
│   ├── disclosure/page.tsx           # 広告表記
│   ├── contact/page.tsx              # お問い合わせ
│   ├── links/page.tsx                # SNS集約ハブ
│   ├── api/
│   │   ├── contact/route.ts
│   │   └── og/route.tsx              # OGP動的生成
│   ├── sitemap.ts
│   ├── robots.ts
│   └── layout.tsx
├── content/
│   └── posts/                        # MDX記事
├── components/
│   ├── mdx/
│   │   ├── Citation.tsx              # 出典表示
│   │   ├── AffiliateCard.tsx         # アフィリLPカード
│   │   ├── Disclaimer.tsx            # 免責表記
│   │   ├── PRBadge.tsx               # PR表記
│   │   ├── Callout.tsx               # 注釈ボックス
│   │   └── ExpertCTA.tsx             # 専門家相談CTA
│   ├── article/
│   ├── layout/
│   └── ui/                           # shadcn/ui
├── lib/
│   ├── affiliate.ts                  # アフィリエイトリンク一元管理
│   ├── seo.ts                        # メタタグ生成
│   ├── jsonld.ts                     # 構造化データ
│   └── utils.ts
├── content-types/
│   └── post.ts                       # 記事の型定義
├── scripts/
│   ├── new-post.ts                   # 記事ひな型生成
│   └── check-citations.ts            # 出典URLの死活確認
├── public/
├── velite.config.ts
├── next.config.mjs
├── tailwind.config.ts
├── tsconfig.json
└── CLAUDE.md
```

### 2.3 開発コマンド

```bash
npm run dev            # 開発サーバー
npm run build          # 本番ビルド
npm run lint           # ESLint
npm run type-check     # 型チェック
npm run new:post       # 新規記事ひな型作成
npm run check:cite     # 出典URL死活確認
```

---

## 3. MDX 記事のスキーマ

### 3.1 frontmatter 必須項目

すべての記事は以下の frontmatter を持つこと。velite のスキーマで型エラーを出すように設定済み。出典が空の記事はビルドが通らない。

```yaml
---
title: 記事タイトル（35文字以内推奨）
slug: kebab-case-slug
description: 検索結果に出る説明文（120文字以内）
publishedAt: 2026-01-15
updatedAt: 2026-01-15
category: 介護 | 相続 | 終活 | お金
tags:
  - 介護保険
  - ケアマネージャー
sources:
  - title: 厚生労働省「介護保険制度の概要」
    url: https://www.mhlw.go.jp/...
    accessedAt: 2026-01-15
hasAffiliate: true
disclaimer: true
author: <<<運営者の表示名>>>
---
```

### 3.2 記事タイプ別の構成テンプレート

#### 制度解説型（基本形）

1. リード文（120字、悩みへの共感→何が分かる記事か）
2. 結論先出し（3〜5行で要点）
3. 制度の概要（出典明記）
4. 具体的なケース・数値（公的資料からの引用）
5. よくある誤解の整理
6. 次のアクション（公的窓口・専門家相談への誘導）
7. 関連記事リンク
8. 出典一覧

#### 比較型（アフィリエイト誘導の主力）

1. リード文＋PR表記
2. 「こんな人におすすめ」で読者の課題明確化
3. サービス概要の客観比較表（自分の主観を入れない）
4. 各サービスの特徴を出典付きで記述
5. 利用前に確認すべきこと
6. 申込フローの一般的な流れ
7. 専門家相談の重要性
8. 関連記事
9. 出典一覧

#### ニュース解説型

1. ニュースの一次情報を引用（出典必須）
2. ターゲット読者にとっての意味
3. 関連する既存制度の整理
4. 想定される影響
5. 専門家相談への誘導
6. 出典一覧

### 3.3 文体・トーン

- 「です・ます調」で統一
- 1文を50字以内、長くても70字を超えない
- 段落は3〜5文で改行
- 専門用語には必ず1行解説を併記
- 漢字とひらがなのバランス: 漢字3割程度
- 「私は」「うちは」など主語は使わない（解説者ポジション維持）
- 読者への呼びかけは「読者の方」「ご家族の状況によって」など客観的に

---

## 4. 共通コンポーネントの仕様

記事内で使う共通コンポーネントは `components/mdx/` に配置済み。MDX で直接使える。

### 4.1 Citation - 出典表示

```mdx
<Citation source="厚生労働省「介護保険制度の概要」" url="https://www.mhlw.go.jp/..." accessedAt="2026-01-15" />
```

数字やデータを引用する箇所には必ず付ける。インライン版とブロック版がある。

### 4.2 AffiliateCard - アフィリエイトリンクカード

```mdx
<AffiliateCard linkId="hokengarden_nenkin" />
```

`lib/affiliate.ts` の定数からデータを引く。直接URLを書かない。`hasAffiliate: true` の記事でのみ使用。使用すると自動でPR表記が記事冒頭に挿入される。

### 4.3 Disclaimer - 免責表記

```mdx
<Disclaimer />
```

すべての記事末尾に配置。「本記事は情報提供を目的とし、個別の状況に応じた助言は専門家にご相談ください」の旨を表示。

### 4.4 PRBadge - PR表記

```mdx
<PRBadge />
```

`hasAffiliate: true` の記事冒頭に自動挿入されるが、明示的に配置することも可能。

### 4.5 Callout - 注釈ボックス

```mdx
<Callout type="info">補足情報</Callout>
<Callout type="warning">注意事項</Callout>
<Callout type="tip">読者向けのヒント</Callout>
```

`type` は info / warning / tip / note の4種。

### 4.6 ExpertCTA - 専門家相談CTA

```mdx
<ExpertCTA category="souzoku" />
```

カテゴリに応じて適切な専門家への相談を促すブロックを表示する。`category` は kaigo / souzoku / shukatsu / okane の4種。

---

## 5. アフィリエイトリンクの管理

### 5.1 一元管理ファイル

すべてのアフィリエイトリンクは `lib/affiliate.ts` に定数として登録する。記事内で直接URLを書くことは禁止。

```typescript
export const AFFILIATE_LINKS = {
  hokengarden_nenkin: {
    asp: 'a8',
    url: 'https://...',
    label: '保険ガーデン年金',
    rewardType: '面談完了',
    category: 'fp_soudan',
    allowedMedia: ['blog'],
  },
  // ...
} as const
```

新しい案件を追加するときは：
1. このファイルに定数追加
2. ASP管理画面で発行された正式リンクを使う
3. `allowedMedia` で掲載可能媒体を明示

### 5.2 媒体ごとの制約（重要）

ASP規約上、媒体によってアフィリエイトリンクの掲載可否が変わる。違反すると報酬没収・アカウント停止のリスクがある。Claude が記事や他のコンテンツを生成するとき、以下を絶対に守る：

| 媒体 | アフィリエイトリンク掲載 | 備考 |
|---|---|---|
| ブログ（このサイト） | OK | A8等にこのドメインを登録済み |
| YouTube概要欄 | 媒体登録時のみOK | A8で別途YouTubeのサイト登録が必要 |
| TikTok | 媒体登録時のみOK | A8で別途TikTokのサイト登録が必要 |
| Instagram | 媒体登録時のみOK | A8で別途Instagramのサイト登録が必要 |
| X (Twitter) | NG | A8では掲載不可 |
| LINE公式（メッセージ・配信） | NG | アフィリエイトリンク直貼り不可 |
| メルマガ | NG | A8では原則不可 |
| note | NG | A8では原則不可 |
| PDF | NG | A8では原則不可 |

**LINEからは自分のブログ記事URLを送ることだけ許可される**。LINE経由で読者をブログに誘導し、ブログ内のアフィリエイトリンクで成果を発生させる。

---

## 6. SEO・YMYL 対応

### 6.1 必須メタ情報

すべての記事ページで以下を出力する（`lib/seo.ts` 経由）：

- title, description, canonical
- OGP（og:title, og:description, og:image, og:type）
- Twitter Card
- robots（index, follow）

### 6.2 構造化データ（JSON-LD）

記事ページには以下の JSON-LD を出力（`lib/jsonld.ts` 経由）：

- Article（headline, datePublished, dateModified, author, image）
- BreadcrumbList
- Organization（サイト全体）
- 必要に応じて FAQPage、HowTo

### 6.3 YMYL 対応の必須要件

介護・相続・終活はYMYL（Your Money or Your Life）領域なので、Google からの信頼性評価が他ジャンルより厳しい。すべての記事で以下を満たす：

1. 著者情報の明記（記事末尾に著者ボックス、運営者ページへのリンク）
2. 最終更新日の表示（GitのコミットからbuildTimeで自動取得）
3. 出典の明示（記事末尾に出典一覧）
4. 専門家への相談誘導（`<ExpertCTA>` または記事内文言）
5. 「個別の判断は専門家へ」の免責文（`<Disclaimer>`）

これらが欠けた記事はビルドエラーまたは警告を出すように `velite.config.ts` を設定済み。

---

## 7. Claude Code の作業ルール

### 7.1 記事生成時の手順

新しい記事を生成するとき、Claude は以下を順守：

1. `npm run new:post` でひな型を生成（または同等の操作）
2. frontmatter を埋める（出典のURLは実在を確認）
3. リサーチ: 公的資料から該当する制度・数値を取得
4. 構成テンプレートに沿って本文を執筆
5. すべての数値・引用に `<Citation>` を付ける
6. 必要に応じて `<AffiliateCard>` `<ExpertCTA>` を配置
7. 記事末尾に `<Disclaimer>` と出典一覧
8. `npm run lint` `npm run type-check` `npm run check:cite` をパスすること

### 7.2 やってはいけないこと（絶対遵守）

- 出典のない数値・統計の記述
- 個人の体験談として書くこと（「私の母が」「うちでは」など）
- 「絶対」「必ず」「確実に」などの断定表現
- 個別具体的な金融助言（「この商品を買うべき」「この保険に入るべき」）
- アフィリエイトリンクを `lib/affiliate.ts` を経由せずに直接URL記載
- 特定の専門家・特定の士業事務所を実名で推奨すること（広告主のサービス除く）
- 競合サイト・他者のコンテンツの構成や文章を模倣すること
- AIで体験談を捏造すること
- ASP規約で許可されていない媒体向けのコンテンツにアフィリリンクを含めること

### 7.3 やるべきこと

- 一次情報（厚労省・国税庁・地方自治体・公的機関）を出典に
- 数値は引用元の発表年を併記
- 制度改正の可能性に触れる
- 読者に応じた選択肢を複数提示する（「Aの場合は〜」「Bの場合は〜」）
- 専門家相談を最後に促す
- 関連記事への内部リンクを2〜3本追加

### 7.4 記事の長さの目安

- 制度解説型: 2,500〜4,000字
- 比較型: 3,500〜5,000字
- ニュース解説型: 1,500〜2,500字
- 動画補足記事: 1,000〜2,000字

長ければよいわけではなく、出典に基づいた密度の高い情報を簡潔にまとめることを優先する。

---

## 8. 短尺動画との連携

### 8.1 動画補足記事の作成

短尺動画（TikTok / Reels / Shorts）の概要欄から誘導するための「動画補足記事」を `/posts/video-{動画ID}` の形で配置する。動画では時間制約で説明しきれない出典・詳細・関連制度をここに集約する。

動画補足記事の構成:
1. 動画埋め込み（lite-youtube-embed で軽量化）
2. 動画で言及した内容の文字起こし要約
3. 出典の詳細
4. 関連する制度の補足
5. 関連記事リンク
6. LINE登録への誘導

### 8.2 リンクハブページ `/links`

`app/links/page.tsx` ですべてのSNS・LINE・主要記事を集約する Linktree 代替ページを実装する。各SNSのプロフィール欄からこのページに誘導することで、外部サービス依存を減らす。

UTMパラメータを各リンクに自動付与し、どのSNSから流入したか計測可能にする。

---

## 9. LINE 公式アカウントとの連携

### 9.1 LINE運用の基本ルール

LINE公式アカウントは「教育配信＋ブログ誘導」専用。アフィリエイトリンクの直貼りは規約違反のため絶対にしない。

### 9.2 ステップ配信の構成

```
1通目: 登録ありがとう＋PDF配布リンク（自社ブログのDLページへ）
2通目: 介護で揉める家族の特徴（自社ブログ記事URL）
3通目: 相続税の基礎知識（自社ブログ記事URL）
4通目: 読者の質問への回答（自社ブログ記事URL）
5通目: 専門家相談を考えるタイミング（自社ブログの比較記事URL）
```

すべての配信メッセージで、リンク先は自社ブログ記事のみ。広告主LPへの直接リンクは含めない。

### 9.3 LINEへの誘導文言

ブログ内・動画内でLINE登録を促す文言は統一する：

> 親の介護お金準備チェックリスト30項目を無料配布中
> LINE登録はこちら → <<<LINE URL>>>

---

## 10. 法的順守事項

### 10.1 ステマ規制（景品表示法）

2023年10月施行のステマ規制に対応する。アフィリエイトリンクを含むコンテンツには PR 表記を明示する：

- ブログ: `<PRBadge>` を記事冒頭に自動挿入
- YouTube: 動画冒頭テロップ＋概要欄＋音声で明示
- TikTok / Reels: 動画内テロップ＋キャプション
- Instagram: キャプション冒頭

### 10.2 景品表示法（優良誤認・有利誤認）

- 客観的根拠のない最上級表現（「業界No.1」「最も〜」）を使わない
- 「必ず痩せる」「絶対に儲かる」など効果を断定しない
- 価格・条件は変動する可能性を明記する

### 10.3 業法ライン（無資格者として絶対超えない領域）

- 金融商品取引法: 個別の投資判断助言、投資勧誘
- 保険業法: 個別の保険商品の募集・勧誘行為
- 税理士法: 個別の税務相談、税務書類作成
- 弁護士法: 個別の法律相談、法律事務
- 司法書士法: 登記申請の代理

「制度として〇〇という仕組みがある」は問題ない。「あなたは〇〇すべき」は危険。

### 10.4 著作権

- 公的資料の引用は出典明示のうえ「主従関係」を守る
- 競合サイト・書籍からの構成・文章のコピーは禁止
- 画像は商用利用可能な素材または自作のみ使用
- 引用箇所は引用符・斜体などで本文と区別する

---

## 11. 運用メトリクス

### 11.1 月次でレビューする指標

ブログ:
- ページビュー、ユニークユーザー
- 流入元別セッション（検索 / SNS / LINE / 直接）
- カテゴリ別人気記事
- 直帰率、平均滞在時間
- アフィリエイトリンクのクリック数（`lib/affiliate.ts` のクリック計測）

LINE:
- 友だち登録数、ブロック率
- ステップ配信の開封率・URLクリック率
- ブロードキャスト配信の反応率

ASP:
- 案件別の発生件数、確定率、確定報酬
- 媒体別（短尺動画→ブログ→ASP の経路別）の成果

### 11.2 KGI / KPI

- KGI: アフィリエイト確定報酬の月次合計
- KPI: LINE友だち登録数 > フォロワー数 > ブログPV

LINE登録数を最重要KPIに置く。フォロワー数より、LINEで継続的に接点を持てる人の数が直接的に収益と相関する。

---

## 12. 変更履歴

このCLAUDE.mdの変更は git で管理する。大きな方針変更時はこのセクションに日付と変更内容を追記する。

- <<<YYYY-MM-DD>>>: 初版作成

---

## 13. 困ったとき

Claude Code が判断に迷う場面では、次の優先順位で判断する：

1. 法律遵守（景表法・業法・著作権）
2. ASP規約遵守
3. 読者への正確で誠実な情報提供
4. SEO・トラフィック最適化
5. 制作効率

上位の項目が下位の項目より常に優先される。トラフィック獲得のために法律やASP規約に反することは絶対にしない。

## イシュー作業の規約
- 必ず plan mode で計画を立ててから実装に入る
- 1 イシュー = 1 ブランチ = 1 PR
- ブランチ名: `<type>/<issue-number>-<slug>` (例: feat/123-oauth-login)
- コミットメッセージ末尾に `Refs #<issue-number>` を入れる
- PR タイトルは Conventional Commits 形式
- PR 本文に `Closes #<issue-number>` を入れて自動クローズ